# 기여 안내

버그 신고와 개선 제안은 GitHub Issues를 이용하십시오.

1. 실제 업무문서, 개인정보, 감사 로그와 사용자 프로필을 커밋하지 않습니다.
2. 기존 출력 형식과 프로필 호환성을 임의로 변경하지 않습니다.
3. 변경 목적, 영향 범위와 검증 결과를 Pull Request에 기록합니다.
4. `python -m unittest discover -s tests -p "test_*.py" -v`를 실행합니다.
5. ETA·OCR·XLS 관련 변경은 해당 회귀 테스트도 실행합니다.
6. 새 의존성을 추가하면 사용 목적과 라이선스를 `THIRD_PARTY_NOTICES.md`에 반영합니다.
7. GUI와 MCP 공용 서비스 변경은 두 실행 경로를 모두 검증합니다.

기여된 코드는 GNU AGPL-3.0 조건으로 배포되는 것에 동의한 것으로 간주합니다.
